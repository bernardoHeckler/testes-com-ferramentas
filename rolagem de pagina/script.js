window.addEventListener('resize', () => {
    document.body.style.minHeight = `${window.innerHeight}px`;
});

// Inicializa a altura no carregamento da página
document.body.style.minHeight = `${window.innerHeight}px`;
